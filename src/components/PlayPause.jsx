import { FaPauseCircle, FaPlayCircle } from "react-icons/fa";

const PlayPause = () => {
  return (
    //     isPlaying && activeSong?.title === song?.title ?
    //      <FaPauseCircle /> :
    //      <FaPlayCircle  />
    <div>dasuashuiodasfu</div>
  );
};

export default PlayPause;
// {isPlaying, activeSong, song, handlePause, handlePlay}
// onClick = {handlePause} size = {35} className = "text-gray-300"
// onClick={handlePlay} size = {35} className = "text-gray-300"
